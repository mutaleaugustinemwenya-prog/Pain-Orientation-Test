export function SiteFooter() {
  return (
    <footer className="border-t border-rule bg-banner">
      <div className="mx-auto max-w-6xl px-4 py-8 sm:px-6">
        <div className="mono-label flex flex-col gap-2 text-[0.65rem] text-text-micro sm:flex-row sm:items-center sm:justify-between">
          <span>Utushimi Registry — a Mockingbird Digital property</span>
          <span>Payments settled via Mobile Money · MTN / Airtel</span>
        </div>
      </div>
    </footer>
  );
}
